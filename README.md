==========================================================
# Web-Technology---511 Assignment
==========================================================
# Richfield Connect

# JS
# document = the current HTML page
# getElementById() = find an HTML element by its ID
# .value = get the value entered into an input
# .innerText = get/change the text inside an HTML element
# const = create a variable that won't be reassigned
# let = create a variable that can be reassigned
# if = check whether something is true
# addEventListener() = wait for something to happen, then run a function
# .preventDefault() = stop the browser's default action
# =      → PUT
# ==     → EQUAL?
# ===    → STRICTLY EQUAL?
# !=     → NOT EQUAL?
# !==    → NOT STRICTLY EQUAL? 